<server xmlns="urn:jboss:domain:17.0">
    <extensions>
        <extension module="org.jboss.as.deployment-scanner"/>
        <!-- Add other necessary extensions here -->
    </extensions>
    <management>
        <management-interfaces>
            <!-- Add management interfaces here -->
        </management-interfaces>
    </management>
    <profile>
        
        <subsystem xmlns="urn:jboss:domain:deployment-scanner:2.0">
            <deployment-scanner path="deployments" relative-to="jboss.server.base.dir" scan-interval="-1"/>
        </subsystem>
        <!-- Add other subsystems here -->
    </profile>
    <interfaces>
        <interface name="management">
            <inet-address value="${jboss.bind.address.management:0.0.0.0}"/>
        </interface>
        <interface name="public">
            <inet-address value="${jboss.bind.address.public:0.0.0.0}"/>
        </interface>
        <!-- Add other interfaces here -->
    </interfaces>
    <socket-binding-group name="standard-sockets" default-interface="public" port-offset="${jboss.socket.binding.port-offset:0}">
        <socket-binding name="management-http" interface="management" port="${jboss.management.http.port:9990}"/>
        <socket-binding name="management-https" interface="management" port="${jboss.management.https.port:9993}"/>
        <socket-binding name="http" port="${jboss.http.port:8080}"/>
        <socket-binding name="https" port="${jboss.https.port:8443}"/>
        <!-- Add other socket bindings here -->
    </socket-binding-group>
</server>